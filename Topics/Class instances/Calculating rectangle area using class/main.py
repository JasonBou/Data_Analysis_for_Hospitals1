# Create a Rectangle class with length and width attributes
class Rectangle:
    def __init__(self, length, width):
        self.length = length
        self.width = width

    # Define an area method to calculate the rectangle's area
    def area(self):
        pass

# Take user input for length and width
length = int(input())
width = int(input())

# Create a Rectangle instance with the given length and width

# Print the area of the rectangle using the area method